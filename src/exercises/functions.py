"""
Function exercises.

Change functions definitions according to the documentation.
Run `pylama src/exercises/functions.py` to run code check.
Run `pytest tests/test_functions.py` to check your solutions.
"""

def last_arg(x):
    """
    Accepts any number of positional arguments and returns the last one.

    Example:

        >> last_arg(1)
        1
        >> last_arg(1, 2, 3)
        3
        >> last_arg(3, 'a', 1)
        1
    """


def to_dict(x):
    """
    Accepts any number of keyword arguments and returns a dict.

    Example:

        >> to_dict()
        {}
        >> to_dict(a=1, b=None)
        {'a': 1, 'b': None}
    """
